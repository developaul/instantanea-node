type Query {
  getPublications(limit: Int!, page: Int!): [Publication]

  getShortPublications(limit: Int!, page: Int!, userId: ID!): [ShortPublication]

  getPublication(publicationId: ID!): Publication
}
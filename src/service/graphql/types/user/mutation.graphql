type Mutation {
  createUser(
    email: String!
    userName: String!
    firstName: String!
    lastName: String!
    password: String!
  ): AuthUser

  authenticateUser(
    email: String!
    password: String!
  ): AuthUser

  updateUser(userId: ID!): User
}